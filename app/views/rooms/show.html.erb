<h1>Action Cable Chat</h1>
<%= link_to "Sign Up", new_user_path if !@current_user%>
<%= link_to "I already have an account", login_path if !@current_user%>
<%= link_to "Log Out", logout_path if @current_user%>
<%= link_to "Profile", user_path(@current_user) if @current_user%>


<h1> Welcome <%=@current_user.name if @current_user%></h1>
<div id="messages">
  <%if @current_user%>
  <p> Your Messages </p>
  <%end%>
  <%=render @messages%>
<div>
